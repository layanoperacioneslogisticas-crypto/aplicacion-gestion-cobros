{
  "name": "cobro-transporte-railway",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix api ci --omit=dev --no-audit --fund=false",
    "start": "npm --prefix api start"
  }
}
